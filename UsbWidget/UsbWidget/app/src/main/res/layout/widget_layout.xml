<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:padding="14dp"
    android:background="@drawable/bg_off">

    <!-- Icône USB -->
    <ImageView
        android:id="@+id/widget_icon"
        android:layout_width="36dp"
        android:layout_height="36dp"
        android:src="@drawable/ic_usb_off"
        android:contentDescription="Modem USB" />

    <!-- Textes -->
    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical"
        android:paddingStart="10dp"
        android:paddingEnd="4dp">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Modem USB"
            android:textSize="13sp"
            android:textStyle="bold"
            android:textColor="#FFFFFFFF" />

        <TextView
            android:id="@+id/widget_status"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Inactif"
            android:textSize="11sp"
            android:textColor="#CCFFFFFF"
            android:layout_marginTop="1dp" />

    </LinearLayout>

    <!-- Toggle image ON/OFF -->
    <ImageView
        android:id="@+id/widget_toggle_img"
        android:layout_width="44dp"
        android:layout_height="26dp"
        android:src="@drawable/toggle_off"
        android:contentDescription="Basculer" />

</LinearLayout>
